{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.firstName, {'attr': {'placeholder': 'Prenom'}}) }}
        {{ form_row(registrationForm.lastName, {'attr': {'placeholder': 'Nom'}}) }}
        {{ form_row(registrationForm.email, {'attr': {'placeholder': 'Email'}}) }}
        {{ form_row(registrationForm.password, {'attr': {'placeholder': 'Mot de passe'}}) }}
        {{ form_row(registrationForm.confirmPassword, {'attr': {'placeholder': 'Confirmez votre mot de passe'}}) }}
        <button type="submit" class="btn">S'inscrire</button>
    {{ form_end(registrationForm) }}
{% endblock %}
